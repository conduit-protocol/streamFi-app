# ── Stellar / Soroban ──────────────────────────────────────────────────────────
# Soroban RPC endpoint
NEXT_PUBLIC_SOROBAN_RPC_URL=https://soroban-testnet.stellar.org

# Stellar network passphrase
NEXT_PUBLIC_NETWORK_PASSPHRASE=Test SDF Network ; September 2015

# Deployed contract addresses (copy from conduit-contracts/.contract-ids/<network>.json)
NEXT_PUBLIC_FACTORY_CONTRACT_ID=
NEXT_PUBLIC_GOVERNOR_CONTRACT_ID=

# Optional: Horizon for account info / balance lookups
NEXT_PUBLIC_HORIZON_URL=https://horizon-testnet.stellar.org

# Optional: multiplier over the network's observed inclusion fee for contract
# transactions. Defaults to 2. Raise it on congested ledgers.
NEXT_PUBLIC_SOROBAN_FEE_MULTIPLIER=2

# ── Demo Mode ──────────────────────────────────────────────────────────────────
# Set to "true" to run the app with mock data (no real contract calls).
# When empty or unset, the app requires a valid NEXT_PUBLIC_FACTORY_CONTRACT_ID.
NEXT_PUBLIC_DEMO_MODE=

# Optional: GraphQL endpoint of the transaction-history subgraph/indexer.
# When unset (and not in demo mode) the Transactions page reports that the
# indexer is not configured instead of showing fabricated data (#592).
NEXT_PUBLIC_SUBGRAPH_URL=

# Optional: Sentry DSN for error tracking. When unset, error tracking
# functions are no-ops (#478).
NEXT_PUBLIC_SENTRY_DSN=

# Optional: withdrawals at or above this amount (display units, e.g. XLM)
# require retyping the amount as an extra confirmation step. Defaults to
# 1000 when unset (#560).
NEXT_PUBLIC_LARGE_WITHDRAWAL_THRESHOLD=
