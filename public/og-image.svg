<svg xmlns="http://www.w3.org/2000/svg" width="1200" height="630" viewBox="0 0 1200 630">
  <!-- Background -->
  <rect width="1200" height="630" fill="#000000"/>

  <!-- Top border accent -->
  <rect y="0" width="1200" height="4" fill="#ffffff"/>

  <!-- Logo mark -->
  <text x="80" y="200" font-family="monospace, sans-serif" font-size="160" font-weight="900" fill="#ffffff">C</text>

  <!-- Product name -->
  <text x="80" y="340" font-family="monospace, sans-serif" font-size="72" font-weight="900" fill="#ffffff">Conduit</text>

  <!-- Tagline -->
  <text x="80" y="420" font-family="monospace, sans-serif" font-size="32" font-weight="400" fill="#888888">Streaming payments on Stellar</text>

  <!-- Divider -->
  <rect x="80" y="460" width="160" height="2" fill="#ffffff"/>

  <!-- Bottom detail -->
  <text x="80" y="510" font-family="monospace, sans-serif" font-size="24" fill="#555555">Per-second token streams · On-chain · Non-custodial</text>

  <!-- Bottom border accent -->
  <rect y="626" width="1200" height="4" fill="#ffffff"/>
</svg>
